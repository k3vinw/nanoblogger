# NanoBlogger Plugin to create excerpt of an entry

# Example1: grabs text up to 1st blank line
NB_EntryExcerpt=`echo "$NB_EntryBody" |sed -e '1,/^$/!d'`

# Example2: strips markup and only grabs text up to 1st blank line
#NB_EntryExcerpt=`echo "$NB_EntryBody" |sed -e 's/<[^>]*>//g; 1,/^$/!d'`

